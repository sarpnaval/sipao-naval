# SIPAO-Naval — dependencias del aplicativo (producción)
# Versiones fijadas: lo que se prueba en local es lo que corre en el servidor.
fastapi==0.139.0
uvicorn==0.51.0
starlette==1.3.1
pydantic==2.13.4
python-multipart==0.0.32   # requerido por la importación de archivos (/api/importar)
openpyxl==3.1.5            # lectura de plantillas .xlsx
huggingface_hub==0.36.0   # respaldo de la base en la instancia publicada
